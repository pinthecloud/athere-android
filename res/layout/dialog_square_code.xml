<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:focusable="true"
    android:focusableInTouchMode="true"
    android:gravity="center"
    android:orientation="vertical"
    android:paddingBottom="@dimen/square_code_dialog_bottom_margin"
    android:paddingTop="@dimen/square_code_dialog_top_margin" >

    <EditText
        android:id="@+id/square_code_dialog_code_edit_text"
        android:layout_width="wrap_content"
        android:layout_height="@dimen/basic_profile_text_height"
        android:layout_marginBottom="@dimen/square_code_dialog_code_enter_button_margin"
        android:ems="10"
        android:gravity="center|bottom"
        android:hint="@string/code"
        android:inputType="text"
        android:maxLength="15"
        android:textSize="@dimen/font_x_large" >
    </EditText>

    <ImageButton
        android:id="@+id/square_code_dialog_enter_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        android:contentDescription="@android:string/ok"
        android:src="@drawable/ok_image_button_bg" />

</LinearLayout>